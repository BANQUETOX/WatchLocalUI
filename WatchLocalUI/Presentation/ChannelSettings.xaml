<Page
    x:Class="WatchLocalUI.Presentation.ChannelSettings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:uen="using:Uno.Extensions.Navigation.UI"
    xmlns:local="using:WatchLocalUI.Presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    //Things to be configurable,
    // 1.Scann date (Calentar input grater than today)
    // 2.Toggle Auto Scann update (Switch)
    // 3.Toogle Only audio channel (Switch)
    // 4.Toggle Scann (Switch)
    
    <Grid Background="Black" Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!-- Header -->
            <RowDefinition Height="*" />
            <!-- Video Grid -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0 0 0 20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Back Button -->
            <Button Grid.Column="0" uen:Navigation.Request="-"
            Width="40" Height="40"
            Background="White"
            Margin="0,0,10,0">
                <FontIcon Glyph="&#xE72B;" Foreground="White" FontSize="24"/>
                <!-- Back icon -->
            </Button>

            <!-- Channel Icon -->
            <Ellipse Grid.Column="1" Width="60" Height="60">
                <Ellipse.Fill>
                    <ImageBrush ImageSource="{Binding ChannelPicturePath}"/>
                </Ellipse.Fill>
            </Ellipse>

            <!-- Channel Info -->
            <StackPanel Grid.Column="2" Margin="10 0" VerticalAlignment="Center">
                <TextBlock Text="{Binding ChannelName}" 
                           Foreground="White" 
                           FontSize="18" 
                           FontWeight="Bold"/>
                <TextBlock Foreground="White" FontSize="14">
                    <Run Text="Starting scan date:"/>
                    <Run Text="{Binding ChannelScannAfter}"/>
                </TextBlock>
            </StackPanel>
        </Grid>
        <!-- Settings -->
        <StackPanel Grid.Row="1">
            <TextBlock Text="Start Scann date"/>
            <DatePicker Header="Set starting scann date" Margin=" -509 0 0 20" SelectedDate="{Binding SelectedDate,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" ></DatePicker>
            <TextBlock Text="Toggle Auto Scann date update"/>
            <ToggleSwitch IsOn="{Binding ChannelAutoScann,Mode =TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0 0 0 20"/>
            <TextBlock Text="Toggle Scann"/>
            <ToggleSwitch IsOn="{Binding ChannelScann,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0 0 0 20"/>
            <TextBlock Text="Toggle Only Audio downloads"/>
            <ToggleSwitch IsOn="{Binding ChannelOnlyAudio,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0 0 0 20"/>
        </StackPanel>

    </Grid>
</Page>
